using System;
using UnityEngine;

namespace Global {
    public class CameraRotate : MonoBehaviour {

        private void Update() {
            //transform.rotation = Quaternion.Euler(0, 0, (float)Math.Acos(GravityController.Instance.normalizedGravity.y));
        }
    }
}
